---
export const prerender = false
import Base from "@layouts/Base.astro"
const { name } = Astro.params
---
<Base title={`Hello ${name}`}>
	<div class="bg-light p-5 rounded mt-3">
		<h1>Hello {name}</h1>
		<p class="lead">What's up?</p>
	</div>
</Base>